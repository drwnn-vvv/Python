P, N = map(int, input().split())

alturas = list(map(int, input().split()))

ganhou = True

for i in range(N - 1):
    if abs(alturas[i+1] - alturas[i]) > P:
        ganhou = False
        break

if ganhou:
    print("YOU WIN")
else:
    print("GAME OVER")
